;;; Bar configuration

(defvar bar-constants `{}`) ;; Don't attempt to `eww update` this

(defwindow bar
  :monitor 0
  :geometry (geometry :x "0%"
                      :y "0%"
                      :width "89.5%"
                      :height "2%"
                      :anchor "top center")
  ;; TODO: X11 support
  :stacking "fg"
  :exclusive true
  :focusable "exclusive"
  :namespace "bar"
  (centerbox
    ""
    (hypr-workspaces)
    ""))
