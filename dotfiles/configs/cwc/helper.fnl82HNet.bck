(local {:spawn_with_shell spawn : screen : client} cwc)

(lambda spawn-app [cmd]
  (spawn (.. "app2unit -- " cmd)))

(lambda percent->pixel [scr num]
  (let [{: width} scr]
    (/ (* num width) 100)))

{: spawn-app : percent->pixel}
